apiVersion: 1

providers:
  - name: 'Gmail Search Agent Dashboards'
    orgId: 1
    folder: 'Gmail Search Agent'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
